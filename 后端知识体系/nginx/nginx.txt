1.更改完nginx配置文件后要reload一下
  sudo nginx -s reload 

2.报错如此,bind() to 0.0.0.0:8080 failed (48: Address already in use),说明其他服务占用了该端口    
  sudo nginx -s stop  关掉服务

3.nginx -t 会去测试你得配置文件得语法，并告诉你配置文件是否写得正确，同时也告诉了你配置文件得路径

4.sudo nginx -c /usr/local/etc/nginx/nginx.conf    -c 指定一个文件，即指定nginx配件文件

5.sudo nginx -s reopen  
  Nginx——Ngine X，是一款自由的、开源的、高性能HTTP服务器和反向代理服务器

6.nginx的功能，负载均衡，反向代理，发布网站，缓存
  起服务，实现跨域

7.mac nginx文件所在的位置
  ／usr/local/etc/nginx
